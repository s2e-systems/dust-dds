name: interoperability

on:
  workflow_dispatch:

jobs:
    interoperability:
        name: OMG Interoperability
        runs-on: ubuntu-latest
        steps:
        - name: Checkout sources
          uses: actions/checkout@v4
        - name: Checkout dds-rtps
          uses: actions/checkout@v4
          with:
            repository: omg-dds/dds-rtps
            ref: 'v1.1.2024'
            path: omg-dds-rtps
            sparse-checkout-cone-mode: false
            sparse-checkout: |
              interoperability_report.py
              test_suite.py
              rtps_test_utilities.py
              requirements.txt
        - uses: dtolnay/rust-toolchain@stable
        - run: cargo build --package dust_dds_shape_main_linux 
        - uses: actions/setup-python@v5
          with:
            python-version: '3.11.4'
        - name: Downloads assets
          uses: robinraju/release-downloader@v1.10
          with:
            repository: omg-dds/dds-rtps
            latest: true
            fileName: "*"
        - name: Unzip
          run: unzip '*.zip' -d executables
        - name: Setting up environment
          run: |
            python3 -m venv .venv
            source .venv/bin/activate
            pip install -r omg-dds-rtps/requirements.txt
        - name: Run P Dust - S Dust
          run: |
            source .venv/bin/activate
            python3 ./omg-dds-rtps/interoperability_report.py -P ./target/debug/dust_dds_shape_main_linux -S ./target/debug/dust_dds_shape_main_linux -o=report.xml -t Test_Domain_0 -v
        # - name: Run P Dust - S Connext
        #   run: python3 ./omg-dds-rtps/interoperability_report.py -P ./target/debug/dust_dds_shape_main_linux -S ./executables/connext_dds-*_shape_main_linux -o=report.xml
        # - name: Run P Connext - S Connext
        #   run: python3 ./omg-dds-rtps/interoperability_report.py -P ./executables/connext_dds-*_shape_main_linux -S ./executables/connext_dds-*_shape_main_linux -o=report.xml
        - name: Upload report
          uses: actions/upload-artifact@v4
          with:
            name: interoperability_report
            path: ./trace.log
        - name: XUnit
          uses: AutoModality/action-xunit-viewer@v1
          with:
            results: ./report.xml
            fail: false
        - name: Upload report
          uses: actions/upload-artifact@v4
          with:
            name: interoperability_report
            path: ./index.html
